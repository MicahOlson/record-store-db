<h1>All Sales Vinyl</h1>

<h3>Available Artists</h3>

<form action="/artists">
  <div class="form-group">
    <label for="search">Search</label>
    <input id="artists_search" name="search" class="form-control" type="text" required>
  </div>
  <button type="submit" class="btn">Go!</button>
</form>

<% if @artists.any? %>
  <ul>
    <% @artists.each do |artist| %>
      <li><a href="/artists/<%= artist.id %>"><%= artist.name %></li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no artists to display.</p>
<% end %>

<p><a href="/artists/new">Add a New Artist</a></p>
<p><a href="/">Return home</a></p>
